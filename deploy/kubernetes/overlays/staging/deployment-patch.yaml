apiVersion: apps/v1
kind: Deployment
metadata:
  name: siprec-server
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: siprec-server
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
