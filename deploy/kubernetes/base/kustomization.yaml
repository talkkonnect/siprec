apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: siprec-server-base

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/version: "1.0.0"

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - rbac.yaml
  - hpa.yaml
